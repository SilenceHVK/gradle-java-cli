ext {
	application = [
		name     : "spring-application",
		mainClass: "me.hvkcoder.application.MySpringApplication"
	]
}

dependencies {
	springLibraries.each { entry -> implementation "$entry.value" }
}