ext {
	application = [
		name     : "spring-application",
		mainClass: "me.hvkcoder.application.MySpringApplication"
	]
}

dependencies {
	springBootLibraries.each { entry -> implementation "$entry.value" }
}